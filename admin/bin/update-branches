#!/bin/sh

set -e

BRANCHES=$(git branch -r | sed 's/ *origin\///' | grep -E '^c[0-9]+$')

git checkout master
git pull -f

for branch in $BRANCHES; do
  git checkout $branch
  git pull -f
  echo
  echo
done
